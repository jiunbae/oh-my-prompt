name: Publish CLI to npm

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3 # v6.0.10

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Extract version from tag
        id: version
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          echo "tag=$TAG_VERSION" >> $GITHUB_OUTPUT

          # Convert date-based tag (YYYY.M.D.N) to semver (YYYY.MDD.N)
          # e.g., 2026.2.9.1 → 2026.209.1
          NPM_VERSION=$(echo "$TAG_VERSION" | node -e "
            const parts = require('fs').readFileSync('/dev/stdin','utf8').trim().split('.');
            if (parts.length === 4) {
              const [y, m, d, n] = parts;
              console.log(y + '.' + m + d.padStart(2,'0') + '.' + n);
            } else {
              console.log(parts.join('.'));
            }
          ")
          echo "npm=$NPM_VERSION" >> $GITHUB_OUTPUT
          echo "Tag: $TAG_VERSION → npm: $NPM_VERSION"

      - name: Update package.json version
        run: |
          cd packages/omp-cli
          node -e "
            const pkg = require('./package.json');
            pkg.version = '${{ steps.version.outputs.npm }}';
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
            console.log('Set version to ' + pkg.version);
          "

      - name: Build CLI package
        run: |
          chmod +x scripts/build-cli.sh
          ./scripts/build-cli.sh

      - name: Verify build
        run: node packages/omp-cli/scripts/verify-build.js

      - name: Create tarball (dry run)
        run: |
          cd packages/omp-cli
          npm pack --dry-run

      - name: Check if version already published
        id: check
        run: |
          NPM_VERSION="${{ steps.version.outputs.npm }}"
          EXISTING=$(npm view "oh-my-prompt@$NPM_VERSION" version 2>/dev/null || echo "")
          if [ -n "$EXISTING" ]; then
            echo "Version $NPM_VERSION already published, skipping"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm
        if: steps.check.outputs.skip != 'true'
        run: |
          cd packages/omp-cli
          npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@fe965f7af51af5f2602596916f38a38df2e33de0 # v3.0.2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ steps.version.outputs.tag }}
          body: |
            ## Installation
            ```bash
            npm install -g oh-my-prompt@${{ steps.version.outputs.npm }}
            ```

            npm version: `${{ steps.version.outputs.npm }}`
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

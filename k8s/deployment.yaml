apiVersion: apps/v1
kind: Deployment
metadata:
  name: oh-my-prompt
  namespace: oh-my-prompt
  labels:
    app: oh-my-prompt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oh-my-prompt
  template:
    metadata:
      labels:
        app: oh-my-prompt
    spec:
      containers:
        - name: oh-my-prompt
          image: your-registry.example.com/oh-my-prompt:latest  # TODO: Replace with your registry
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: oh-my-prompt-config
            - secretRef:
                name: oh-my-prompt-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10

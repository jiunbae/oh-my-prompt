apiVersion: v1
kind: ConfigMap
metadata:
  name: prompt-analyzer-config
  namespace: prompt-analyzer
data:
  MINIO_ENDPOINT: "minio.example.com"  # TODO: Replace with your MinIO endpoint
  MINIO_BUCKET: "claude-prompts"
  MINIO_USE_SSL: "true"
